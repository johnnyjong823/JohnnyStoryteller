/**
 * 分類與標籤的控制字彙（白名單）。
 *
 * 這是「故事越來越雜」的唯一防線 —— 故事文字由 LLM 產生，
 * 它每次都會重新發明標籤（勇敢/勇氣/有勇氣/不怕）。
 * 白名單 + zod 驗證讓打錯字在 build 時就失敗，而不是在說故事時才發現。
 *
 * ⚠ 新增標籤要是「有意識的動作」：先確認現有的真的涵蓋不了，再加進來。
 * ⚠ 這份清單必須整份貼進 Manus 的提示詞（見 docs/08）。
 */

/** 分類：每篇單選。上限 12 個 —— 想加第 13 個之前，先想能不能歸進現有的。 */
export const CATEGORIES = [
  '經典童話',
  '品格養成',
  '生活習慣',
  '情緒認識',
  '自然與動物',
  '科學探索',
  '節慶與文化',
  '家庭與朋友',
  '安全教育',
] as const;

/** 標籤：每篇 3–6 個。可以慢慢長，但每次新增都要是刻意的。 */
export const TAGS = [
  // 情緒
  '勇敢', '害怕', '生氣', '難過', '開心', '嫉妒', '緊張',
  // 關係
  '家人', '朋友', '分享', '道歉', '合作', '陌生人',
  // 場景
  '森林', '海洋', '學校', '夜晚', '下雨', '城市', '農場',
  // 品格與主題
  '誠實', '耐心', '努力', '好奇', '負責任', '同理心',
] as const;

export type Category = (typeof CATEGORIES)[number];
export type Tag = (typeof TAGS)[number];
